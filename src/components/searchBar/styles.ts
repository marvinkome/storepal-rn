import { StyleSheet } from 'react-native';

export const search = StyleSheet.create({
    container: {
        marginTop: 10,
        backgroundColor: 'transparent',
        borderTopWidth: 0,
        borderBottomWidth: 0,
    },
    input: {
        backgroundColor: '#fff',
    },
});
