import { StyleSheet } from 'react-native';
import { color } from '../../constants';

export default StyleSheet.create({
    header: {
        backgroundColor: '#fff',
        marginTop: 10,
    },
    text: {
        color: color.normal,
        fontSize: 17,
        fontWeight: '500',
    },
    icon: {
        color: color.normal,
    },
});
